# Trinity Overnight Cycle Log

## 2026-04-03

### 01:15 (continuation)
- **Thought**: After reviewing dashboards.md, the highest-impact validated work is the analytics infrastructure gaps, specifically Gap #1: Stage Change History. Building a generic `opportunity_change` event store will enable all time-based reporting for Hoffmann's utilization dashboard and Netic's 5-dashboard suite.
- **Problem Fit**: Directly addresses the need to track when opportunities move through stages (for revenue, pipeline, capacity views). Validated by multiple stakeholders (Thomaz, Davin, Justin from Hoffmann).
- **Simplicity**: Uses existing effect handler pattern; adds one generic table + handler instead of many specific ones. Leverages `custom_fields` JSONB in existing opportunity table where appropriate.
- **Maintenance Cost**: Low - effect handler runs automatically on opportunity updates; no cron jobs. Schema stable (generic blob captures future events).
- **Bloat Signals**: None. Pure SQL + TypeScript, within current stack.
- **Utility Score**: 9/10 (enables 5 dashboards, foundational for capacity/utilization reporting).

- **Action**:
  - Design `greenhouse.opportunity_change` table schema (opportunity_id, account_id, change_type, changed_at, changed_by, previous_value, new_value, metadata JSONB)
  - Implement effect handler in backend to capture stage changes, status changes, value updates
  - Backfill from existing version history to seed initial data
  - Add query helpers / repository methods to fetch stage durations and transition timelines
- **Result**: Schema drafted, effect handler stub created, backfill SQL prepared. Build artifacts in `trinity/experiments/analytics-infra/`.
- **Next**: Continue implementation tomorrow night; target Phase 1 complete (stage transitions tracked). Then move to Gap #2: close date/status fields. Ensure integration with Hoffmann utilization board by end of week.
