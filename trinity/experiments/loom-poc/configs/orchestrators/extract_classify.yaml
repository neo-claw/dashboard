name: extract_classify
timeout_seconds: 300
pipeline_stages:
- name: extract
  worker_type: extractor
  input_mapping:
    text: goal.context.text
- name: classify
  worker_type: classifier
  input_mapping:
    text: extract.result
